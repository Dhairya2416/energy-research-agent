services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: energy-research-agent
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - ./knowledge_base:/app/knowledge_base
    restart: unless-stopped

